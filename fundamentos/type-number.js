// tipo Number

const meuNumero = 3;

const n1 = 1;
const n2 = 2;

const soma = n1 + n2;

console.log(`Resultado da soma: ${soma}`);

/*
 *ponto flutuante
 */

const float = 3.3;
const nf = 0.5; //não precisa colocar 0.5

const divisao = `divisão é: ${n1 / float}`;

console.log(divisao);

/*
 *NaN -> Not A Number (não é um número)ponto flutuante
 */

const alura = "Alura";
console.log(`não é número: ${alura * n2}`);
