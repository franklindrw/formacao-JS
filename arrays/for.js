const poderDeLuta = [
    1000,
    2000,
    3000,
    4000,
    5000,
    6000,
    7000,
    8000,
    "Error: Mais de 8000",
];

for (let i = 0; i < poderDeLuta.length; i++) {
    console.log(i, `Poder de Luta: ${poderDeLuta[i]}`);
}
